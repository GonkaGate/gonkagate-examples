services:
  cli:
    image: gonkagate/gonkagate-chat-cli:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    stdin_open: true
    tty: true
    command: ["chat", "--stream=true"]
